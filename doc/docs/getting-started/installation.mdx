---
sidebar_position: 1
---
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# Installation

To start using **ScrollJs**, you have two easy options:

- **Install it via a package manager**
- **Use a CDN link directly in your HTML file**

## 1. Install via Package Manager

Make sure you have **Node.js** installed on your system.

Then, open your terminal and run:
<Tabs>
  <TabItem value="npm" label="npm" default>
    ```bash
    npm install @charmingdc/scrolljs
    ```
  </TabItem>
  <TabItem value="yarn" label="yarn">
    ```bash
    yarn add @charmingdc/scrolljs
    ```
  </TabItem>
  <TabItem value="bun" label="bun">
    ```bash
    bun add @charmingdc/scrolljs
    ```
  </TabItem>
</Tabs>

Once installed, **ScrollJs** is ready to use in your project.

## 2. Use via CDN

You don’t need to install anything — just add one of the following `<script>` tags to your HTML file.

### Option A: From jsDelivr (Recommended)

Choose between two file formats:

<Tabs>
  <TabItem value="es-module" label="ES Module" default>
    ```html
    <script src="https://cdn.jsdelivr.net/npm/@charmingdc/scrolljs@2.0.0/dist/scroll.esm.min.js" type="module"></script>
    ```
  </TabItem>
  <TabItem value="commonjs" label="CommonJs">
    ```html
    <script src="https://cdn.jsdelivr.net/npm/@charmingdc/scrolljs@2.0.0/dist/scroll.umd.min.js"></script>
    ```
  </TabItem>
</Tabs>

**Optional Fallback:**
For broader browser support, you can add both formats conditionally:

```html
<script type="module" src="https://cdn.jsdelivr.net/npm/@charmingdc/scrolljs@2.0.0/dist/scroll.esm.min.js"></script>

<script nomodule src="https://cdn.jsdelivr.net/npm/@charmingdc/scrolljs@2.0.0/dist/scroll.umd.min.js"></script>
```

:::info **info**
You can replace `2.0.0` with your preferred version. Check the [CHANGELOG.md](https://github.com/Charmingdc/ScrollJs/CHANGELOG.md) for version updates.
:::



### Option B: From GitHub (Latest on main branch)

Loads the latest build directly from GitHub:

```html
<script src="https://cdn.jsdelivr.net/gh/Charmingdc/ScrollJs@main/scroll.min.js"></script>
```

:::tip **Tip**
Use the versioned link (Option A) in production for better stability.
:::

:::note **CDN Usage Note** 
After adding the script tag, ScrollJs class: `ScrollObserver` will be available globally in your HTML project. If using the UMD file, it may be available as `window.ScrollObserver`.
:::


### What’s next? 🚀  
Learn how to initialize **ScrollJs** in your project. 
